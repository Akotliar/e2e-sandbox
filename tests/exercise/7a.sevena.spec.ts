import { test, expect } from '@playwright/test';

test.describe('', () => {

/**
   **Criteria:** When a user is reviewing their cart items in the https://www.razer.com/ online store and then switches tabs the messaging in 
   the tab is changed to something like  "Going AFK?...". verify that the messaging change occurs in the tab.
*/

  test('', async ({ page }) => {
    await page.goto("")

    //need to complete this,
  });
});
